<%= form_for @ryde do |f| %>
    <%= f.collection_select(:starting_location, Destination.all, :name, :name, {prompt: "Starting Location"}, {include_blank: true, required: true}) %>
    <br>
    <%= f.collection_select(:ending_location, Destination.all, :name, :name, {prompt: "Ending Location"}, {include_blank: true, required: true}) %>
    <br>
    <%= f.submit 'Edit Ryde Request' %>
    <br>
<% end %>